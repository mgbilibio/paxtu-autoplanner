name = "paxtu-xai-proxy"
main = "src/index.ts"
compatibility_date = "2026-09-04"
workers_dev = true

# github.io sempre liberado. Localhost só se ALLOW_LOCALHOST não for "false".
[vars]
ALLOW_LOCALHOST = "false"
